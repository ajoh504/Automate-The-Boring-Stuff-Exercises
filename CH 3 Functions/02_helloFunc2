# Here we define a function and give it one parameter, 'name'
# Parameters are variables that hold arguments and pass them into
# the code block of the function.

def hello(name):
    print('Hello, ' + name)
hello('Alice')
hello('Bob')
